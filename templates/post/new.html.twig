{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <h1>Create new Post</h1>

    {{ include('post/_form.html.twig', {button_label: 'Create' }) }}

    <div class="mt-3">  
        <a class="btn btn-primary d-inline" href="{{ path('post_index') }}">List of Posts</a>
    </div>
{% endblock %}
